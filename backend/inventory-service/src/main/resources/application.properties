

server.port=${PORT:8083}

spring.datasource.url=jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require
spring.datasource.username=${SUPABASE_DB_USER:postgres.ruokqqdgbrzxnttrxvgd}
spring.datasource.password=${SUPABASE_DB_PASSWORD:thyagaA123456@}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.data-source-properties.prepareThreshold=0
spring.datasource.hikari.data-source-properties.preparedStatementCacheQueries=0

# For development you can keep 'update'. For production prefer 'validate' + migrations.
spring.jpa.hibernate.ddl-auto=${INVENTORY_JPA_DDL_AUTO:update}
spring.jpa.open-in-view=false

order.service.url=${ORDER_SERVICE_URL:http://localhost:8080}
order.service.integration.enabled=${ORDER_SERVICE_INTEGRATION_ENABLED:true}


